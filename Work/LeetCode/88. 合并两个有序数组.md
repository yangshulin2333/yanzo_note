- “非递减顺序”是一个数学和计算机科学中常用的术语，用于描述特定的排列方式。它在排序和算法分析中很常见。

- 非递减顺序表示数组中的元素按照从小到大的顺序排列，但允许相邻的元素相等。因此，数组中的每个后续元素不小于前一个元素。这意味着数组可能是递增的，但也允许重复的值。
	例如，以下都是非递减顺序的数组：
	- `[1, 2, 2, 3, 4]`
	- `[5, 5, 6, 7]`
	- `[1, 1, 1, 2, 3]`