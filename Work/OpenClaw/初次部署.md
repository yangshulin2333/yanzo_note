[零度博客](https://www.freedidi.com/23445.html)

下载Windows Terminal
https://apps.microsoft.com/detail/9n0dx20hk701?hl=zh-CN

### 一、安装 WSL2

在 PowerShell（管理员）执行：

```
wsl --install
```


## 使用国内镜像源（最简单、推荐）

国内可以通过 **HF-Mirror** 镜像站来下载模型。你只需要在运行指令前，先在 WSL 终端里执行一行命令来设置环境变量：

Bash

```
export HF_ENDPOINT=https://hf-mirror.com
```

然后再运行你的 vLLM 启动指令：

Bash

```
python -m vllm.entrypoints.openai.api_server \
  --model Qwen/Qwen2.5-7B-Instruct-AWQ \
  --quantization awq_marlin \
  --gpu-memory-utilization 0.8 \
  --max-model-len 16384 \
  --enable-auto-tool-choice \
  --tool-call-parser hermes
```